#
# Pre-requis : OPENCV
# opencv doit etre installe dans /usr/local via le flag CMAKE_INSTALL_PREFIX=/usr/local
# cf opencvinstall.txt
#

# Construire le projet
mkdir build
cd build
export OpenCV_DIR=/usr/local/lib/cmake/opencv4
cmake ..
make
